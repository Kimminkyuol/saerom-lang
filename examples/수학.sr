# 12-모듈.sr 이 가져다 쓰는 모듈

원주율은 3.14이다.

수의 절댓값이라는 것은:
    만약 수가 0보다 작으면:
        0에서 수를 뺀 값을 돌려준다.
    수를 돌려준다.

수를 곱으로 거듭하다라는 것은:
    답은 1이다.
    1부터 곱까지의 번마다 반복한다:
        답은 답에 수를 곱한 값이다.
    답을 돌려준다.

수가 양수이다라는 것은:
    수가 0보다 큰지를 돌려준다.
